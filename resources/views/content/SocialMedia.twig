<link href="{{ plugin_path('SocialMedia') }}/css/shariff.complete.css" rel="stylesheet">
<link href="{{ plugin_path('SocialMedia') }}/css/SocialStyles.css" rel="stylesheet">
<script async src="{{ plugin_path('SocialMedia') }}/js/shariff.complete.js"></script>

{% set socialMediaData = config("SocialMedia.social.data") | split(', ') %}
{% set dataServices = "" %}
{% set socialTheme = config("SocialMedia.social.theme") %}
{% set socialFixed = config("SocialMedia.social.fixed") %}

<div class="shariff socialbar hidden-sm-down pull-xs-left"

    {% for i in 0..socialMediaData|length %}
        {% if i != socialMediaData|length %}
            {% if i == 0 %}
                {% set dataServices = dataServices ~ "\"" ~ socialMediaData[i] ~ "\"" %}
            {% else %}
                {% set dataServices = dataServices ~ ",\"" ~ socialMediaData[i] ~ "\"" %}
            {% endif %}
        {% endif %}
    {% endfor %}

    data-services='[{{ dataServices }}]'
    {% if socialFixed == "true"  %}
      data-title='{{ trans("SocialMedia::Template.socialText") }}'
      data-url='{{ trans("SocialMedia::Template.socialUrl") }}'
      data-media-url='{{ trans("SocialMedia::Template.socialMediaUrl") }}'
    {% endif %}
    data-theme='{{ socialTheme }}'
    data-lang='{{ services.sessionStorage.getLang() }}'>

</div>
